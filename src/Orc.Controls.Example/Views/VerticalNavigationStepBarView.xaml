<catel:UserControl x:Class="Orc.Controls.Example.Views.VerticalNavigationStepBarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:catel="http://schemas.catelproject.com"
        xmlns:xamlbehaviors="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:orccontrols="clr-namespace:Orc.Controls;assembly=Orc.Controls"
        Background="{DynamicResource Orc.Brushes.GrayBrush10}">

    <Grid x:Name="layoutRoot" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <orccontrols:StepBar x:Name="stepbar" Grid.Row="0" Grid.RowSpan="5" Orientation="Vertical" Items="{Binding Items}" />

        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Content="Previous" Command="{Binding MoveBackItem}" CommandParameter="{Binding ElementName=stepbar}" />
            <Button Content="Next" Command="{Binding MoveForwardItem}" CommandParameter="{Binding ElementName=stepbar}" />
        </StackPanel>

    </Grid>
 </catel:UserControl>
