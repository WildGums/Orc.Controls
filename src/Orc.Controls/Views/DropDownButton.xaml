<catel:UserControl x:Class="Orc.Controls.DropDownButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:catel="http://catel.codeplex.com"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{Binding Header}" />
            <StackPanel Grid.Column="1">
                <ToggleButton Name="DropDownToggleButton" Content="push" IsChecked="{Binding IsDropDownOpen}"/>
                <Popup Placement="Bottom" PlacementTarget="{Binding ElementName=DropDownToggleButton}"                       
                       IsOpen="{Binding IsChecked, ElementName=DropDownToggleButton}" StaysOpen="True">
                    <ListBox ItemsSource="{Binding Items}"/>
                </Popup>
            </StackPanel>
            
        </Grid>
    </Grid>
</catel:UserControl>
