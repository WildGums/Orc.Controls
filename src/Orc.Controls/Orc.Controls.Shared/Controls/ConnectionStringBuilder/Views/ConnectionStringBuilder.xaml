<catel:UserControl x:Class="Orc.Controls.ConnectionStringBuilder"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:catel="http://catel.codeplex.com"
                   xmlns:controls="clr-namespace:Orc.Controls"
                   mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400">
    
    <catel:StackGrid>
        <catel:StackGrid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </catel:StackGrid.ColumnDefinitions>

        <TextBox IsReadOnly="True" 
                 Text="{Binding DisplayConnectionString, Mode=OneWay}" Margin="0"
                 BorderBrush ="{Binding ConnectionState, Converter={controls:ConnectionStateToColorBrushValueConverter}, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Margin="0" Content="..." Command="{Binding Edit}" ToolTip="Edit connection string"/>
        <Button Content="x" Margin="0" Command="{Binding Clear}" ToolTip="Clear"/>
    </catel:StackGrid>
</catel:UserControl>
 
